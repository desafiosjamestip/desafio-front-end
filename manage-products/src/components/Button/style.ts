import { Button } from "@mui/material";
import styled from "styled-components";

export const StyledButton = styled(Button)`
  && {
    background-color: #118879;
    width: 145px;
  }
  &&:hover {
    background-color: #0d6a5e;
  }
`;
