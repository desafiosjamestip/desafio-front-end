import { Header, PageContainer } from "../../components";
import { ProductList } from "../../components/ProductList";

export const Stock = () => {
  return (
    <PageContainer>
      <Header />
      <ProductList />
    </PageContainer>
  );
};
