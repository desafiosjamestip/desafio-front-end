import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import {
  withKnobs,
  text,
  number,
  boolean,
  select,
} from '@storybook/addon-knobs';
import { FormControl } from './FormControl';
import { TextField } from '../TextField';

<Meta title="Design System/FormControl" decorators={[withKnobs]} />

# FormControl

FormControl has the role to use a default space between a form elements.

<Canvas>
  <Story name="FormControl" decorators={[withKnobs]}>
    <FormControl>
      <TextField id="name" label="Name" />
    </FormControl>
    <FormControl>
      <TextField id="last-name" label="Last Name" />
    </FormControl>
  </Story>
</Canvas>

## Props

<ArgsTable of={FormControl} />
